/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "avatars.githubusercontent.com",
      "www.iconpacks.net",
    ],
  },
};

export default nextConfig;
